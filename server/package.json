{
  "name": "bunkaio-quiz-stripe",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker — capture des leads du quiz Bunkaio et synchronisation Stripe Customer (CRM commercial).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "stripe": "^17.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
